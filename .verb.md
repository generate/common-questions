# {%= name %} {%= badge("fury") %} {%= badge("travis") %}

> {%= description %}

This is currently formatted to work with [question-store][], but this will probably be generalized so it can work with [inquirer][], [question-cache][] etc.

## Install
{%= include("install-npm", {save: true}) %}

## Usage

```js
var commonQuestions = require('{%= name %}');

var Questions = require('question-store');
var questions = new Questions();

var obj = commonQuestions();

for (var key in obj) {
  questions.visit(key, obj[key]);
}
```

## API
{%= apidocs("index.js") %}

## Related projects
{%= related(verb.related.list) %}  

## Running tests
{%= include("tests") %}

## Contributing
{%= include("contributing") %}

## Author
{%= include("author") %}

## License
{%= copyright() %}
{%= license() %}

***

{%= include("footer") %}

{%= reflinks(['question-cache', 'inquirer', 'question-store']) %}